name: app_dat_ban
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ^3.8.0

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: ^0.20.2         # ✅ sửa version này
  http: ^0.13.6
  image_picker: ^1.0.4
  shared_preferences: ^2.2.2
  geolocator: ^10.1.0
  url_launcher: ^6.1.10
  qr_flutter: ^4.0.0
  socket_io_client: ^2.0.3 

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  flutter_launcher_icons: ^0.13.1

flutter_icons:
  android: true   
  ios: true
  image_path: "assets/images/logoN.png"  # Đường dẫn đến file icon của bạn

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/imgChiNhanh/
    - assets/imgUuDai/
    - assets/imgPayment/
